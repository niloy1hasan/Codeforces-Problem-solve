#include <stdio.h>

int main() {
  
  int w;
  scanf("%d", &w);
  int a,b,c,n, solution = 0;
  
  while(w!=0) {
    scanf("%d %d %d", &a,&b,&c);
    n = a + b + c;
    
    if(n>=2) {
      solution++;
    } 
   w--;
  }  
  
  printf("%d", solution);
  
  return 0;
}
